<UserControl x:Class="NickAI.App.Views.ModelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <TextBlock Text="Models" Style="{StaticResource H1}" />
        <TextBlock Text="NICK runs local models through Ollama on http://localhost:11434. Only capabilities the runtime reports are shown."
                   Style="{StaticResource Muted}"
                   TextWrapping="Wrap"
                   Margin="0,6,0,0" />
      </StackPanel>
      <Button Grid.Column="1"
              Content="Refresh"
              Style="{StaticResource PrimaryButton}"
              VerticalAlignment="Top"
              Command="{Binding RefreshCommand}" />
    </Grid>

    <Border Grid.Row="1" Style="{StaticResource GlassPanel}" Margin="0,0,0,12" Padding="12">
      <StackPanel>
        <TextBlock Text="{Binding Status}" TextWrapping="Wrap" />
        <TextBlock Style="{StaticResource Muted}" Margin="0,6,0,0">
          <Run Text="Active model:" />
          <Run Text="{Binding ActiveModel, Mode=OneWay}" />
        </TextBlock>
      </StackPanel>
    </Border>

    <TextBlock Grid.Row="2"
               Text="No models loaded."
               Style="{StaticResource Muted}"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" />

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVis}}">
      <ItemsControl ItemsSource="{Binding Items}" Margin="0,0,8,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource GlassPanel}" Margin="0,0,0,10" Padding="14">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                  <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                  <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,0">
                    <Run Text="{Binding ParameterSize, Mode=OneWay}" />
                    <Run Text="·" />
                    <Run Text="{Binding Quantization, Mode=OneWay}" />
                    <Run Text="·" />
                    <Run Text="{Binding SizeDisplay, Mode=OneWay}" />
                  </TextBlock>
                  <TextBlock Text="{Binding Capabilities}" Style="{StaticResource Muted}" Margin="0,4,0,0" />
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Use"
                        Style="{StaticResource GhostButton}"
                        VerticalAlignment="Center"
                        Command="{Binding DataContext.UseModelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}" />
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
