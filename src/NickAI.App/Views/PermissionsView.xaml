<UserControl x:Class="NickAI.App.Views.PermissionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NickAI.App.ViewModels">

  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,16">
      <TextBlock Text="Permission center" Style="{StaticResource H1}" />
      <TextBlock Text="Control what NICK may do. ALLOW runs without asking, ASK prompts before each high-impact action, DENY blocks it."
                 Style="{StaticResource Muted}"
                 TextWrapping="Wrap"
                 Margin="0,6,0,0" />
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Rows}" Margin="0,0,8,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource GlassPanel}" Margin="0,0,0,8" Padding="14">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                  <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding Description}" Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,12,0" />
                </StackPanel>

                <ComboBox Grid.Column="1"
                          Style="{StaticResource ComboBoxFlat}"
                          MinWidth="120"
                          VerticalAlignment="Center"
                          ItemsSource="{Binding Source={x:Static vm:PermissionRowViewModel.Modes}}"
                          SelectedItem="{Binding Mode}" />
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
