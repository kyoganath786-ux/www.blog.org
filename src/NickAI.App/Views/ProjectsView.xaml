<UserControl x:Class="NickAI.App.Views.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <TextBlock Text="Projects" Style="{StaticResource H1}" />
        <TextBlock Text="Open a folder NICK can read, build and analyse. The active project receives new artifacts and build logs."
                   Style="{StaticResource Muted}"
                   TextWrapping="Wrap"
                   Margin="0,6,0,0" />
      </StackPanel>
      <Button Grid.Column="1"
              Content="＋  Add project"
              Style="{StaticResource PrimaryButton}"
              VerticalAlignment="Top"
              Command="{Binding AddProjectCommand}" />
    </Grid>

    <TextBlock Grid.Row="1"
               Text="No projects yet. Add a folder to get started."
               Style="{StaticResource Muted}"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" />

    <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}" Visibility="{Binding IsEmpty, Converter={StaticResource InverseBoolToVis}}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource GlassPanel}" Margin="0,0,0,10">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                <TextBlock Text="{Binding Kind}" Style="{StaticResource Muted}" Margin="0,4,0,0" />
                <TextBlock Text="{Binding RootPath}" Style="{StaticResource Muted}" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,4,0,0" />
              </StackPanel>

              <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Use"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding DataContext.UseProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}" />
                <Button Content="Open folder"
                        Style="{StaticResource GhostButton}"
                        Margin="8,0,0,0"
                        Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}" />
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</UserControl>
